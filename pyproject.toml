[project]
name = "synthetic-data-generator"
version = "0.1.0"
description = "LLM-powered synthetic dataset generator"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0.0",
    "requests>=2.31.0",
    "streamlit>=1.30.0",
]

[project.scripts]
synthgen = "app:main"
